% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_manifest.R
\name{write_manifest}
\alias{write_manifest}
\title{Write a web app manifest to the Shiny www directory}
\usage{
write_manifest(
  meta,
  path = "www",
  display = "standalone",
  start_url = "/",
  background_color = NULL
)
}
\arguments{
\item{meta}{Either a path to a YAML file or a named list.  The same object
you pass to \code{social_meta()} works here.}

\item{path}{Directory to write \code{manifest.json} into.  Defaults to
\code{"www"} relative to the working directory.}

\item{display}{Browser display mode.  One of \code{"standalone"},
\code{"fullscreen"}, \code{"minimal-ui"}, or \code{"browser"}.  Defaults
to \code{"standalone"}.}

\item{start_url}{Start URL passed to the manifest.  Defaults to \code{"/"}.}

\item{background_color}{Background colour shown on the splash screen while
the app loads.  Defaults to \code{theme_color} when set, otherwise
\code{"#ffffff"}.}
}
\value{
The path to the written file, invisibly.
}
\description{
Generates a \href{https://developer.mozilla.org/en-US/docs/Web/Manifest}{Web
App Manifest} (\code{manifest.json}) and writes it to \code{www/} so Shiny
can serve it at \code{/manifest.json}.  Call this once in \code{global.R}
before the app starts.
}
\details{
Reference \code{manifest.json} from the UI by passing
\code{manifest = "/manifest.json"} to \code{social_meta()}.
}
